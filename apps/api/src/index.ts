// In a Node.js application, when the module is called via a require statement,
// the module's exports from the file named in the main property will be what's returned to
// the Node.js application.

import getAPIName from './getAPIName';

export default getAPIName;
